/// Moved to `lib/widgets/freshness_indicator.dart` — shared with child detail and the live
/// trip map, not owned by the dashboard (docs/06-development/PROJECT_STRUCTURE.md).
///
/// Re-exported here only so this path keeps resolving; new code should import the shared
/// location directly.
export '../../../widgets/freshness_indicator.dart';
