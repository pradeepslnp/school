/// Moved to `lib/widgets/journey_status_chip.dart` — shared with child detail, the live trip
/// map, and journey history, not owned by the dashboard
/// (docs/06-development/PROJECT_STRUCTURE.md).
///
/// Re-exported here only so this path keeps resolving; new code should import the shared
/// location directly.
export '../../../widgets/journey_status_chip.dart';
